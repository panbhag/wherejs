making it a node module
versioning
where
	number	
		between
	String:
		finalize the behaviour of the case insensitive(true or false) for all string comparisons
		if value is a regex it should match accordingly
	Array
		match particular array index
		match particular elements in array which specify certain conditions
	Element: $exists, $mod, $type
	when size is $gt 4 someting like that
	value: value/function


later
indexing
linq like syntax

tests
	Array: $elemMatch, $size
	Javascript: $where, $regex


done
----
where
	filtering an array of strings/numbers etc ie basic types when there is no key value pairs
	deep compare
	comparison: $ne,$lt,$lte,$gt,$gte,$in,$ni,$all
	shortcut comparison: "x.y": 2, instead of x:{y:2}
	logical: $or,$and, $not, $nor
	Array: $elemMatch, $size
	Javascript: $where, $regex
	condition is a function
	String:
		case insensitive search: iMatch
		contains
		startsWith
		endsWith

		
	
tests	
	deep compare
	comparison: $ne,$lt,$lte,$gt,$gte,$in,$ni,$all
	shortcut comparison: "x.y": 2, instead of x:{y:2}
	logical: $or,$and, $not, $nor
	Array: $elemMatch, $size
	condition is a function
	String:
		case insensitive search: iMatch
		contains
		startsWith
		endsWith

documentation
website


